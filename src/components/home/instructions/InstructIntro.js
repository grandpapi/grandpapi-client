import React from 'react';
import { PinkSpan, IntroSection } from '../../../styles';

export default function InstructIntro() {
  return (
    <IntroSection>
      <h2>Mega Nap is the easiest way to make a fullstack application without having to write a lick of backend code. <PinkSpan>Here’s how it works:</PinkSpan></h2>
    </IntroSection>
  );
}
